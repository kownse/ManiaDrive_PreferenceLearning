level_track_mat = cell2mat(feel_track);
[m,n] = size(level_track_mat);
train_index = m * 4 / 5;
train_level = level_track_mat(1:train_index,2:n);
train_level_label = level_track_mat(1:train_index,1);
test_level = level_track_mat(train_index+1:m,2:n); 
test_level_label = level_track_mat(train_index+1:m,1);
[train_level_scale,ps] = mapminmax(train_level');
ps.ymin = 0;
ps.ymax = 1;
[train_level_scale,ps] = mapminmax(train_level', ps);
train_level_scale = train_level_scale';
[test_level_scale, ps] = mapminmax(test_level');
ps.ymin = 0;
ps.ymax = 1;
[test_level_scale,ps] = mapminmax(test_level', ps);
test_level_scale = test_level_scale';
model = svmtrain(train_level_label,train_level_scale, '-c 2 -g 1 -t 2');
svmpredict(test_level_label, test_level_scale, model);